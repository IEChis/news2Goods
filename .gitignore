# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.swh?

# Dev server logs (runtime, not source)
.dev.err
.dev.log

# Local env / secrets (defensive)
.env
.env.*
# 但允许提交 .env.example 作为配置模板（不含真实密钥）
!.env.example

# Server-side config backups (may contain keys in other setups)
server-config.backup.json
# 本机服务配置（含真实 Coze 工作流 ID），不进仓库；用 server-config.example.json 作模板
server-config.json

# 评测引擎实测脚本的临时编译产物（由 `npm run eval:harness` 生成）
.harness-build/
.harness-build2/
scripts/debug-eval.cjs

# 本地诊断截图与临时调试文件（不入仓库）
_local/
_diag-*.png
*.diag.png

