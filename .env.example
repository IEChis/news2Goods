# ── 大模型网关密钥（aigw.yuexiuproperty.cn）──
# 复制本文件为 .env 并填入你的真实 key；.env 已被 .gitignore 忽略，不会提交到仓库。
# 真实密钥只存在于本机 .env，由 dev 服务器在 /api/llm 服务端兜底使用，前端包与 git 历史都不会出现。
AIGW_API_KEY=在此填入你的 aigw apiKey

# ── Coze 凭证（api.coze.cn）──
# PAT 仅用于本地 Dev 代理（/api/coze、/api/coze-trial），不进前端包 / 历史。
# 去 Coze 控制台「个人访问令牌」页生成，粘贴到下面。
COZE_PAT=在此填入你的 Coze Personal Access Token（pat_ 开头）

# ── Coze 工作流 ID（可选）──
# 三个独立工作流（抓热点 / 匹配商品 / 生成文案）的 ID。
# 前缀 VITE_ 的会被注入到前端打包产物（如需前端 Coze 功能可用，填真实 ID）；
# 不带 VITE_ 的仅 dev 服务端（/api/coze 代理、/api/coze-trial）使用。
# 仓库源码默认是占位符 "<COZE_WORKFLOW_ID>"，不填则 Coze 调用会明确报错。
VITE_COZE_WF_GETNEWS=<你的 getNews 工作流 ID>
VITE_COZE_WF_MATCHGOODS=<你的 matchGoods 工作流 ID>
VITE_COZE_WF_CREATECOPY=<你的 createCopy 工作流 ID>
COZE_WF_GETNEWS=<你的 getNews 工作流 ID>
COZE_WF_MATCHGOODS=<你的 matchGoods 工作流 ID>
COZE_WF_CREATECOPY=<你的 createCopy 工作流 ID>
